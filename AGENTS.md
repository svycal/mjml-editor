# Agent Instructions

## Package Manager

Always use `pnpm` instead of `npm` for running scripts and installing packages.

```bash
# Correct
pnpm install
pnpm run dev
pnpm run build

# Incorrect
npm install
npm run dev
npm run build
```
